/* 
length: retorna o tamanho da String
indexOf: retorna a primeira posição encontrada do caractere passado como paramêtro
lastIndexOf: retorna a última posição encontrada do cartactere passado como paramêtro
toUpperCase: retorna uma nova string convertendo as letras para maiúsculas
toLowerCase: retorna uma nova string convertendo as letras para maiúsculas
charAt: retorna o caractere na posição passada por paramêtro
charCodeAt: retorna o código com base na posição passada por paramêtro
fromCharCode: retorna um caractere com base no código passado por paramêtro
includes: retorna verdadeiro se a string contém a string passada por paramêtro
starWith: retorna verdadeiro se a string inicia com a string passada por paramêtro
endsWith: retorna verdadeiro se a string termina com a string passada por paramêtro
localeCompare: retorna -1 se a string passada por paramêtro for maior, 0 se for igual e 1 se for menor
match: retorna partes da string com base na RegExp passada por paramêtro
search: retorna a primeira posição encontrada, com base na RegExp passada por paramêtro
replace: retorna uma nova string resultante da substituição da string ou RegExp passada no primeiro paramêtro pelo segundo paramêtro
slice: retorna uma parte da string que está invocando a função, iniciando na posição passada no primeiro paramêtro até a posição final passada no segundo parâmetro, ou da posição passada no primeiro parâmetro até o fim, caso o segundo parâmetro não seja informado
split: retorna um array contendo o resultado da divisão da string original de acordo com o critério passado por parâmetro
substring: similar ao slice, não aceita valores negativos como parâmetro e permite a inversão dos parâmetros
concat: retorna uma nova string resultante da concatenação da que está invocando a função e da outra, passada por parâmetro 
padStart: completa a string com a caracteres no início
padEnd: completa a string com caracteres no fim
repeat: repete um caractere
trim: elimina espaços cm branco no início e no fim
trimLeft: elimina espaços em branco no início
trimRight: elimina espaços em branco no fim

*/