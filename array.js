// Em JS, um array é apenas um objeto que oferece operações para acessar e
// manipular suas propriedades.

// Criando um array por meio da função construtora:
const languages = new Array("Python", "C", "Java");
console.log(languages);

// A propriedade length indica a quantidade de elementos que existem dentro do Array

